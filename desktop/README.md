# Signal Web Desktop

Windows x64 桌面壳，安全加载生产 HTTPS 客户端，并通过 Electron `setOverlayIcon` 在 Windows 任务栏显示未读数字。

```bash
npm install
npm run package:win
```

产物位于 `dist/SignalWeb-win32-x64/`。发布压缩包为 `SignalWeb-Windows-x64-v1.1.0.zip`，解压后运行 `SignalWeb.exe`，无需安装。
